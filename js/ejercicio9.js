/**9. Arrays básicos: Declara un array con 5 nombres y muestra cada nombre en la
consola usando un ciclo. */
let nombres = ['Aida', 'Pablo', 'Sebas', 'Diana', 'Omis'];
for (let i = 0; i<nombres.length; i++) {
    console.log(nombres[i]);
}